# usage
```sh
git clone git@github.com:mikio815/dotfiles.git

cd dotfiles

ln -s ./starship.toml ~/.config/starship.toml
ln -s ./.wezterm.lua ~/.wezterm.lua
```

`eval "$(starship init zsh)"` をzshrcに追加


